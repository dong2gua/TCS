<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:regions="http://prismlibrary.com/"
        xmlns:inf="clr-namespace:ThorCyte.Infrastructure.Commom;assembly=ThorCyte.Infrastructure"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d" 
        x:Class="TestProtocol.MainWindow"
        Title="{Binding CaptionString}" Width="900" Height="730" Style ="{StaticResource {x:Type Window}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderBrush="DarkGray" BorderThickness="1">
            <StackPanel  Orientation="Horizontal" Margin="10,0">
                <Button Content="Open" Command="{Binding LoadExpCommand}" Margin="5,2,5,2"/>
                <Button Content="Save" Command="{Binding SaveCommand}" Margin="5,2,5,2"/>
            </StackPanel>
        </Border>
        
        <ContentControl Grid.Row="1" x:Name="ProtocolRegion" regions:RegionManager.RegionName="{x:Static inf:RegionNames.ProtocolRegion}"/>
    </Grid>
</Window>
