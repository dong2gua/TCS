<UserControl x:Class="ThorCyte.ProtocolModule.Views.Modules.ChannelMod"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Expander Header="Channel" IsExpanded="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Channel :" Margin="2"/>
            <ComboBox Grid.Column="1" Margin="2" ItemsSource="{Binding ChannelNames}" SelectedItem="{Binding SelectedChannel}"/>
        </Grid>
    </Expander>
</UserControl>
