<UserControl x:Class="ThorCyte.HeaderModule.Views.TabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="150">
    <Grid IsEnabled="{Binding IsLoaded}">
        <!--<Border BorderThickness="1" CornerRadius="5" BorderBrush="CadetBlue">-->
            <StackPanel >
            <RadioButton GroupName="tab"  Margin="4" Height="100" Width="80" FontSize="20" Content="Review" Command="{Binding TabCommand}" CommandParameter="ReviewModule" Style="{StaticResource {x:Type ToggleButton}}" IsChecked="{Binding IsLoaded, Mode=OneWay}">
            </RadioButton>
            <RadioButton GroupName="tab"  Margin="4" Height="100" Width="80" FontSize="20" Content="Analysis" Command="{Binding TabCommand}" CommandParameter="AnalysisModule" Style="{StaticResource {x:Type ToggleButton}}" >
            </RadioButton>
        </StackPanel>
        <!--</Border>-->
    </Grid>
</UserControl>
