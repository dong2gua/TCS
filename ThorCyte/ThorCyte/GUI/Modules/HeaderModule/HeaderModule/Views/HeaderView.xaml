<UserControl x:Class="ThorCyte.HeaderModule.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
         <Menu>
            <MenuItem Header="File">
                <MenuItem Header="Open..." Command="{Binding OpenCommand}"></MenuItem>
                <MenuItem Header="Close" Command="{Binding CloseCommand}"></MenuItem>
            </MenuItem>
            <MenuItem Header="Settings"></MenuItem>
        </Menu>
        <Border BorderThickness="1" Grid.Row="1" CornerRadius="5" BorderBrush="CadetBlue">
        <WrapPanel Margin="5" >
            <Button Margin="2" Width="70" Height="35" FontSize="18" Command="{Binding TabCommand}" CommandParameter="ReviewModule">Review</Button>
            <Button Margin="2" Width="70" Height="35" FontSize="18" Command="{Binding TabCommand}" CommandParameter="AnalysisModule">Analysis</Button>
        </WrapPanel>
        </Border>
    </Grid>
</UserControl>
