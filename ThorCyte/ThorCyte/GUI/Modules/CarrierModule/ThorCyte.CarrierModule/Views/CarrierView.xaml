<UserControl x:Class="ThorCyte.CarrierModule.Views.CarrierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" x:Name="Uct">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Width="{Binding ElementName=Uct,Path=ActualWidth}">
            <ContentControl Content="{Binding ShowingView}"/>
        </Grid>
        <Expander Grid.Row="1" IsExpanded="False" Header="Tile View">
            <ScrollViewer  Width="{Binding ElementName=Uct,Path=ActualWidth}" 
                      VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ContentControl Content="{Binding ShowingTile}" BorderThickness="3"/>
            </ScrollViewer>
        </Expander>
    </Grid>
</UserControl>
